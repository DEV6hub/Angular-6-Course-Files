import { StructuralUnlessDirective } from './structural-unless.directive';

describe('StructuralUnlessDirective', () => {
  it('should create an instance', () => {
    const directive = new StructuralUnlessDirective();
    expect(directive).toBeTruthy();
  });
});
