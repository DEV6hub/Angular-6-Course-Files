import { ShirtFilterPipe } from './shirt-filter.pipe';

describe('ShirtFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ShirtFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
